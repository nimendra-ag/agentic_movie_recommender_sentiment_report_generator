# Secrets — never commit
.env

# Strip notebook outputs before committing (install: pip install nbstripout)
# Run once to activate: nbstripout --install
*.ipynb filter=nbstripout

# Python
__pycache__/
*.py[cod]
.venv/
